# Model Zoo Downloader

This script downloads the pre-trained weights for the MV-MAE model.

## Usage
1. Open terminal in this folder.
2. Run the script:
   ```bash
   chmod +x download_models.sh
   ./download_models.sh